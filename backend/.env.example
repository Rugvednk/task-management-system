# DATABASE_URL — set this to your PostgreSQL or SQLite connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/task_management"

# For SQLite (no DB server needed):
# DATABASE_URL="file:./dev.db"
